clear && echo "Đang Bắt Đầu Cài Đặt" && sleep 1s && clear && pkg install git -y && git clone https://github.com/TranVanHoang2000/v1.git && cd v1 && chmod +x * && echo 'PATH="$PATH:$HOME/v1"' >> $HOME/.bashrc && source $HOME/.bashrc && clear && echo "Đã Cài Đặt Thành Công" && sleep 1s && clear && mv hd motd
