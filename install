clear && echo "Đang Bắt Đầu Cài Đặt" && apt update && apt upgrade -y && sleep 0.1 && clear && pkg install git -y && git clone https://github.com/TranVanHoang2000/v1.zip && cd v1.zip && pkg install unzip -y && unzip v1.zip && echo 'PATH="$PATH:$HOME/v1.zip"' >> $HOME/.bashrc && source $HOME/.bashrc && chmod a+x exit && chmod a+x hd && chmod a+x open && chmod a+x psiphon-tunnel-core && chmod a+x psiphon1 && chmod a+x psiphon2 && chmod a+x psiphonfree && echo "Đã Cài Đặt Thành Công" && sleep 0.1 && hd
